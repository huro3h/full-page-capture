# Changelog

このプロジェクトの主な変更点を記録する。
形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に従い、
バージョニングは [セマンティック バージョニング](https://semver.org/lang/ja/) に従う。

## [Unreleased]

## [1.1.1] - 2026-09-11

### Changed

- アイコンを線画ベースの図案に変更。背景を持たない透過PNGにし、
  ワークスペースの他の拡張機能(`stray-tab-closer` など)と同じ作法に揃えた。
  図案の元データ `assets/icons/icon.svg` を同梱する

## [1.1.0] - 2026-09-11

### Added

- **高速モード**（既定）。`chrome.debugger` 経由で CDP の
  `Page.captureScreenshot({captureBeyondViewport:true})` を1回呼び、
  ページ全体を一度に撮る。長いページで5〜10倍速くなる
  (MDNの長いリファレンスで 31.4秒 → 6.4秒)
- 設定ページで撮影方法を「高速」「連写」から選べるように

### Changed

- 既定の撮影方法が連写から高速へ。次の場合は自動で連写へ切り替わる:
  本文がiframeやスクロール領域にある場合、画像の辺が 64000px を超える場合

## [1.0.0] - 2026-09-11

### Added

- ページ全体を1枚のPNGとして撮影する機能 (ツールバーのアイコンをクリック)
- 表示されている範囲だけを撮影する機能
- 撮影結果を確認して **PNG / JPEG / PDF** として保存する結果ページ
- PDFは幅をA4に合わせて埋め込む。既定は「1ページにまとめる」で、「A4縦で分割」も選択可能
  (1ページ形式はPDFのページ上限14400ptを超える場合、自動的にA4分割へ切り替わる)
- 保存ファイル名のテンプレート設定 (設定ページ)。`%TITLE%` `%DOMAIN%` `%DATE%` `%TIME%`
  など14種の変数、ライブプレビュー、よく使う組み合わせのプリセット、最大文字数の指定
- スクロール対象の自動判定。ページ本体がスクロールしない場合、
  中のスクロールコンテナや同一オリジンiframeを探して撮影し、その領域だけを切り出す
  (例: HackMD の `/community/` ビューはノート本体が同一オリジンiframeに入っている)
- 撮影中の自動処理: スクロールバーの非表示、`scroll-behavior: smooth` の無効化、
  アニメーション・トランジションの停止、`background-attachment: fixed` の解除
- 固定要素の扱い: sticky要素は貼り付きを解除して素の位置に流し、
  上端の固定ヘッダーは1枚目にだけ残し、浮遊ウィジェットや下端バーは隠す
- キーボードショートカット用コマンド2種。**デフォルトの割り当ては意図的に持たない**
  (他のショートカットと衝突させないため。`chrome://extensions/shortcuts` から手動で設定する)
- 合成画像のピクセルを直接検証するE2Eテスト (`test/e2e.js`)
- 撮影できないページ (Chrome ウェブストア、`chrome://`、他の拡張機能のページ) を
  事前に判定し、バッジとツールチップで理由を表示
